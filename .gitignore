# Dipendenze dei banchi (npm install)
node_modules/

# Fotografie che i banchi scrivono nella radice a ogni giro
/barra-*.png
/schermo-*.png

# Uscite dei generatori, da guardare prima di copiarle a mano
/nuove/
/dizionario.js

# I diari che l'emulatore Firebase scrive nella radice quando gira
# `firebase emulators:exec` (vedi docs/AUDIT-TECNICO-2026-09-17.md).
/firestore-debug.log
/ui-debug.log
/database-debug.log
/pubsub-debug.log

# Cache di Python (genera.py carica i dizionari con importlib)
__pycache__/
